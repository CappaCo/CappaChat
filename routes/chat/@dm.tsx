import { Head } from "fresh/runtime";

import { define } from "@/utils.ts";

export default define.page(function () {

    return (
        <>
            <Head>
                <title>DMs</title>
            </Head>
            <h1>DM page</h1>
        </>
    );
});
